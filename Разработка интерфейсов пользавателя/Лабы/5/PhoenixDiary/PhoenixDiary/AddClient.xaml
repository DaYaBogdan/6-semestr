<Window x:Class="PhoenixDiary.AddClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление клиента" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ФИО:" Margin="0,0,0,5"/>
        <TextBox x:Name="FIOTextBox" 
                 Grid.Row="1" 
                 Margin="0,0,0,15"
                 Padding="8"
                 FontSize="14"/>

        <TextBlock Grid.Row="2" Text="Номер телефона:" Margin="0,0,0,5"/>
        <TextBox x:Name="PhoneTextBox" 
                 Grid.Row="3" 
                 Margin="0,0,0,20"
                 Padding="8"
                 FontSize="14"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton" 
                    Click="Cancel_Click" 
                    Content="Отмена" 
                    Margin="0,0,10,0" 
                    Padding="15,8"/>
            <Button x:Name="SaveButton" 
                    Click="Save_Click" 
                    Content="Сохранить" 
                    Padding="15,8"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>