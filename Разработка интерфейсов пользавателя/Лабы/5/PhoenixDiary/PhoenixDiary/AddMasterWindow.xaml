<Window x:Class="PhoenixDiary.AddMasterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление мастера" Height="380" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Логин -->
        <TextBlock Grid.Row="0" Text="Логин:" Margin="0,0,0,5"/>
        <TextBox x:Name="LoginTextBox" 
                 Grid.Row="1" 
                 Margin="0,0,0,15"
                 Padding="8"
                 FontSize="14"/>

        <!-- Пароль -->
        <TextBlock Grid.Row="2" Text="Пароль:" Margin="0,0,0,5"/>
        <PasswordBox x:Name="PasswordBox" 
                     Grid.Row="3" 
                     Margin="0,0,0,15"
                     Padding="8"
                     FontSize="14"/>

        <!-- Подтверждение пароля -->
        <TextBlock Grid.Row="4" Text="Подтверждение пароля:" Margin="0,0,0,5"/>
        <PasswordBox x:Name="ConfirmPasswordBox" 
                     Grid.Row="5" 
                     Margin="0,0,0,20"
                     Padding="8"
                     FontSize="14"/>

        <!-- Выбор роли -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Роль:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="RoleComboBox" Width="120" SelectedIndex="0">
                <ComboBoxItem Content="master" Tag="master"/>
                <ComboBoxItem Content="admin" Tag="admin"/>
            </ComboBox>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton" 
                    Click="Cancel_Click" 
                    Content="Отмена" 
                    Margin="0,0,10,0" 
                    Padding="15,8"/>
            <Button x:Name="SaveButton" 
                    Click="Save_Click" 
                    Content="Сохранить" 
                    Padding="15,8"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>