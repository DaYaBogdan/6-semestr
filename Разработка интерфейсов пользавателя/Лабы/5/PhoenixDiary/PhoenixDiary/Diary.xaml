<Window x:Class="PhoenixDiary.Diary"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PhoenixDiary"
        Title="PhoenixDiary – Еженедельник" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <local:SidebarControl x:Name="Sidebar" 
                              Grid.Column="0"
                              DiaryClick="Sidebar_DiaryClick"
                              ClientsClick="Sidebar_ClientsClick"
                              MastersClick="Sidebar_MastersClick"
                              AllTasksClick="Sidebar_AllTasksClick"
                              LogoutClick="Sidebar_LogoutClick"/>

        <!-- Основной контент (еженедельник) -->
        <Border Grid.Column="1" Background="#F5F5F5">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Кнопки Добавить/Удалить -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button x:Name="AddButton" Click="OpenAddModal" Margin="0,0,10,0" Padding="10,5" Width="100">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Добавить"/>
                            <TextBlock Text="+" Margin="5,0,0,0" FontSize="14" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <Separator Grid.Row="1" Margin="0,0,0,10"/>

                <!-- Заголовки дней недели -->
                <Grid Grid.Row="2" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="ПН" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="ВТ" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="СР" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Grid.Column="3" Text="ЧТ" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Grid.Column="4" Text="ПТ" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Grid.Column="5" Text="СБ" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Grid.Column="6" Text="ВС" FontWeight="Bold" TextAlignment="Center"/>
                </Grid>

                <!-- Дни недели с задачами -->
                <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="WeekDaysControl" ItemsSource="{Binding WeekDays}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                </Grid>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="LightGray" BorderThickness="0.5" Margin="2" Padding="5" Background="White">
                                    <StackPanel>
                                        <TextBlock Text="{Binding FormattedDate}" FontSize="12" TextAlignment="Center" Foreground="Gray"/>
                                        <Separator Margin="0,5"/>
                                        <ItemsControl ItemsSource="{Binding Tasks}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,2" Padding="5" CornerRadius="4">
                                                        <StackPanel>
                                                            <TextBlock FontWeight="Bold" Text="{Binding ClientName}" FontSize="12"/>
                                                            <TextBlock Text="{Binding ServiceName}" FontSize="10" Foreground="Gray"/>
                                                            <TextBlock Text="{Binding TimeRange}" FontSize="10" Foreground="DarkGreen"/>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Навигация по неделям -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button x:Name="PrevWeekButton" Click="PrevWeek" Content="← Предыдущая" Margin="5" Padding="10,5"/>
                    <Button x:Name="ResetTodayButton" Click="ResetToToday" Content="Вернуться" Margin="5" Padding="10,5"/>
                    <Button x:Name="NextWeekButton" Click="NextWeek" Content="Следующая →" Margin="5" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>