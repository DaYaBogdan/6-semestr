<Window x:Class="PhoenixDiary.ClientsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Список клиентов – PhoenixDiary" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель кнопок -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <!-- Кнопка Добавить -->
            <Button x:Name="AddButton" 
                    Click="OpenAddModal_Click"
                    Margin="0,0,10,0" 
                    Padding="10,8"
                    Width="100">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Добавить" VerticalAlignment="Center"/>
                    <TextBlock Text="+" Margin="5,0,0,0" FontSize="14" FontWeight="Bold"/>
                </StackPanel>
            </Button>

            <!-- Кнопка Удалить -->
            <Button x:Name="DeleteButton" 
                    Click="DeleteSelectedClients_Click"
                    Margin="0,0,10,0" 
                    Padding="10,8"
                    Width="100"
                    IsEnabled="False">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Удалить" VerticalAlignment="Center"/>
                    <TextBlock Text="🗑️" Margin="5,0,0,0" FontSize="12"/>
                </StackPanel>
            </Button>

            <!-- Кнопка Выбрать все / Снять все -->
            <Button x:Name="SelectAllButton" 
                    Click="SelectAll_Click"
                    Padding="10,8"
                    Width="120">
                <TextBlock x:Name="SelectAllText" Text="Выбрать все"/>
            </Button>

            <!-- Счётчик выбранных -->
            <TextBlock x:Name="SelectedCountText" 
                       Text="Выбрано: 0" 
                       VerticalAlignment="Center" 
                       Margin="20,0,0,0" 
                       Foreground="Gray"/>
        </StackPanel>

        <!-- Разделитель -->
        <Separator Grid.Row="1" Margin="0,0,0,10"/>

        <!-- Таблица клиентов -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Заголовок таблицы (показывается, если есть клиенты) -->
                <Border x:Name="HeaderPanel" 
                        Visibility="Collapsed"
                        BorderBrush="#CCCCCC" 
                        BorderThickness="0,0,0,2"
                        Margin="0,0,0,5"
                        Padding="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="1" 
                                   Text="ФИО" 
                                   FontWeight="Bold" 
                                   Margin="20,0,0,0"/>
                        <TextBlock Grid.Column="2" 
                                   Text="Номер телефона" 
                                   FontWeight="Bold"/>
                    </Grid>
                </Border>

                <!-- Список клиентов -->
                <ItemsControl x:Name="ClientsListControl" ItemsSource="{Binding Clients}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E0E0E0" 
                                    BorderThickness="0,0,0,1" 
                                    Padding="5"
                                    Background="Transparent">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F5F5F5"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Чекбокс выбора -->
                                    <CheckBox Grid.Column="0" 
                                              IsChecked="{Binding IsSelected}"
                                              VerticalAlignment="Center"
                                              Checked="ClientCheckBox_Checked"
                                              Unchecked="ClientCheckBox_Unchecked"/>

                                    <!-- ФИО клиента -->
                                    <TextBlock Grid.Column="1" 
                                               Text="{Binding FIO}" 
                                               VerticalAlignment="Center"
                                               Margin="20,0,0,0"/>

                                    <!-- Телефон -->
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Phone}" 
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Сообщение, если клиентов нет -->
                <Border x:Name="EmptyStatePanel" 
                        Visibility="Collapsed"
                        Background="#F9F9F9" 
                        Padding="40" 
                        Margin="0,20,0,0">
                    <TextBlock Text="Нет добавленных клиентов" 
                               HorizontalAlignment="Center" 
                               Foreground="#999"
                               FontSize="14"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>